function markFavourite(){

}
//invited
const storageInput = document.querySelector('.storage');
const invited = document.querySelector('.invited');
const button = document.querySelector('.button');
if(storageInput){
  name.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  invited.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', invited.textContent)
}
button.addEventListener('click', saveToLocalStorage)

//name
const storageInput = document.querySelector('.storage');
const name = document.querySelector('.name');
const button = document.querySelector('.button');
if(storageInput){
  name.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  name.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', name.textContent)
}
button.addEventListener('click', saveToLocalStorage)

//name1
const storageInput = document.querySelector('.storage');
const name1 = document.querySelector('.name1');
const button = document.querySelector('.button');
if(storageInput){
  name1.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  name1.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', name1.textContent)
}
button.addEventListener('click', saveToLocalStorage)

//date1
const storageInput = document.querySelector('.storage');
const date1 = document.querySelector('.date1');
const button = document.querySelector('.button');
if(storageInput){
  date1.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  date1.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', date1.textContent)
}
button.addEventListener('click', saveToLocalStorage)

//date2
const storageInput = document.querySelector('.storage');
const date2 = document.querySelector('.date2');
const button = document.querySelector('.button');
if(storageInput){
  date2.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  date2.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', date2.textContent)
}
button.addEventListener('click', saveToLocalStorage)

//date3
const storageInput = document.querySelector('.storage');
const date3 = document.querySelector('.date3');
const button = document.querySelector('.button');
if(storageInput){
  date3.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  date3.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', date3.textContent)
}
button.addEventListener('click', saveToLocalStorage)

//address
const storageInput = document.querySelector('.storage');
const address = document.querySelector('.adress');
const button = document.querySelector('.button');
if(storageInput){
  address.textContent = storedInput
}
storageInput.addEventListener('input',letter => {
  address.textContent = letter.target.value
})
const saveToLocalStorage = () => {
  localStorage.setItem('textinput', address.textContent)
}
button.addEventListener('click', saveToLocalStorage)
